#include <cc50.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#define _ZOPEN_SOURCE

int main(int argc, char *argv[]){
    if(argc != 2){
        return 1;
    }
    string salt = "";
    for(int i = 0; i < 2;i++){
        
  
    }
    printf("%s",crypt("guest",salt));
}
